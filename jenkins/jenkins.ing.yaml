apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: jenkins-web-ui
  namespace: uat
spec:
  rules:
  - host: cicddemo.jenkins.test.io
    http:
      paths:
      - backend:
          serviceName: jenkins-web-ui
          servicePort: 80
